/** Provenance strings shown next to every number in the UI. */
export const EQUIPMENT_DATASET = {
  dataset: "MTA elevator/escalator equipment index (data/index.json)",
  query: "src/lib/index listStations()",
};

export const OUTAGE_HISTORY_DATASET = {
  dataset: "MTA Elevator and Escalator Availability, monthly (data.ny.gov rc78-7x78)",
};

export const LIVE_DATASET = {
  dataset: "MTA current elevator and escalator outages (NYCT ENE feed)",
};

export const MTA_STATUS_URL = "https://new.mta.info/elevator-escalator-status";
